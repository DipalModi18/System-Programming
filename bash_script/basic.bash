#!/bin/bash

echo Name of the program running $0
echo Values of all command line arguments $*
echo Total number of command line arguments $#
echo Process ID of the current process $$
echo Exit status of most recent command $?
echo PID of most recent background process $!
